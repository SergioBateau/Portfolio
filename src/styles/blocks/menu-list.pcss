.menu-list {
    display: flex;
    align-items: center;
    color: white;
    font-size: 16px;
    font-weight: 400;
}
.menu-item {
    margin-right: 60px;
    &:last-child {
        margin-right: 0;
    }
}