.menu-list {
    display: flex;
    align-items: center;
    font-weight: 400;
}
.menu-item {
    margin-right: 60px;
    @include desktop {
        margin-right: 30px;
    }
    @include tablets {
        display: none;
    }
    &:last-child {
        margin-right: 0;
    }
}